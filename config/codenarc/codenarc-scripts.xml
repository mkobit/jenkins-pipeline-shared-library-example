<?xml version="1.0"?>
<ruleset xmlns="http://codenarc.org/ruleset/1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://codenarc.org/ruleset/1.0 http://codenarc.org/ruleset-schema.xsd"
         xsi:noNamespaceSchemaLocation="http://codenarc.org/ruleset-schema.xsd">

  <description>CodeNarc rules for scripts/ utility Groovy scripts — NoDef enforced</description>

  <ruleset-ref path="rulesets/basic.xml"/>
  <ruleset-ref path="rulesets/braces.xml"/>
  <ruleset-ref path="rulesets/convention.xml">
    <!-- Scripts use dynamic dispatch; explicit return/param types would require full imports -->
    <exclude name="MethodReturnTypeRequired"/>
    <exclude name="VariableTypeRequired"/>
    <exclude name="ParameterTypeRequired"/>
    <!-- NoDef is intentionally NOT excluded here: use final instead of def for local variables -->
    <!-- conflicts with UnnecessaryReturnKeyword from unnecessary.xml -->
    <exclude name="ImplicitReturnStatement"/>
    <!-- false positive: string templates intentionally contain ${} expressions -->
    <exclude name="GStringExpressionWithinString"/>
    <!-- scripts don't use @CompileStatic -->
    <exclude name="CompileStatic"/>
  </ruleset-ref>
  <ruleset-ref path="rulesets/exceptions.xml"/>
  <ruleset-ref path="rulesets/groovyism.xml"/>
  <ruleset-ref path="rulesets/imports.xml"/>
  <ruleset-ref path="rulesets/unnecessary.xml"/>
  <ruleset-ref path="rulesets/unused.xml"/>

</ruleset>
