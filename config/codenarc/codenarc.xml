<?xml version="1.0"?>
<ruleset xmlns="http://codenarc.org/ruleset/1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://codenarc.org/ruleset/1.0 http://codenarc.org/ruleset-schema.xsd"
         xsi:noNamespaceSchemaLocation="http://codenarc.org/ruleset-schema.xsd">

  <description>CodeNarc rules for Jenkins shared library Groovy code</description>

  <ruleset-ref path="rulesets/basic.xml"/>
  <ruleset-ref path="rulesets/braces.xml"/>
  <ruleset-ref path="rulesets/convention.xml">
    <!-- vars/ scripts use implicit `call` methods — not traditional class structure -->
    <exclude name="MethodReturnTypeRequired"/>
    <exclude name="VariableTypeRequired"/>
    <exclude name="ParameterTypeRequired"/>
    <exclude name="NoDef"/>
    <!-- conflicts with UnnecessaryReturnKeyword from unnecessary.xml -->
    <exclude name="ImplicitReturnStatement"/>
    <!-- false positive: Jenkins pipeline scripts passed as string literals intentionally contain ${} expressions -->
    <exclude name="GStringExpressionWithinString"/>
    <!-- Jenkins shared library src classes cannot be @CompileStatic: they use CPS-transformed
         dynamic dispatch and interact with the pipeline context via dynamic Groovy -->
    <exclude name="CompileStatic"/>
  </ruleset-ref>
  <ruleset-ref path="rulesets/exceptions.xml"/>
  <ruleset-ref path="rulesets/groovyism.xml"/>
  <ruleset-ref path="rulesets/imports.xml"/>
  <ruleset-ref path="rulesets/unnecessary.xml"/>
  <ruleset-ref path="rulesets/unused.xml"/>

  <!-- Jenkins CPS / Serializable rules (Enhanced Classpath Rules — requires Jenkins
       JARs on CodeNarc's compilationClasspath, which the shared-library plugin
       provides automatically when both plugins are applied together).

       All Jenkins rules default to applyToFileNames='Jenkinsfile', so they must be
       explicitly configured to run on shared library .groovy source files. -->
  <ruleset-ref path="rulesets/jenkins.xml">

    <rule-config name="ClassNotSerializable">
      <property name="applyToFileNames" value="*.groovy"/>
      <!-- vars/ scripts and test classes are not subject to CPS serialization requirements -->
      <property name="doNotApplyToFilesMatching" value=".*/vars/.*|.*/test/.*"/>
    </rule-config>

    <rule-config name="ClosureInGString">
      <property name="applyToFileNames" value="*.groovy"/>
    </rule-config>

    <!-- cpsPackages: this rule only fires for classes whose package starts with one of
         the configured prefixes. Without it, the rule silently skips all packaged classes
         and only fires in the default package (a bare Jenkinsfile).
         cpsPackages is a List<String> and cannot be set via XML property value; configure
         it in build.gradle.kts instead:
           tasks.withType<CodeNarc>().configureEach {
               doFirst { (rules.find { it.name == "CpsCallFromNonCpsMethod" } as? AbstractRule)
                   ?.setProperty("cpsPackages", listOf("com.mkobit")) }
           } -->
    <rule-config name="CpsCallFromNonCpsMethod">
      <property name="applyToFileNames" value="*.groovy"/>
    </rule-config>

    <rule-config name="ExpressionInCpsMethodNotSerializable">
      <property name="applyToFileNames" value="*.groovy"/>
    </rule-config>

    <rule-config name="ForbiddenCallInCpsMethod">
      <property name="applyToFileNames" value="*.groovy"/>
    </rule-config>

    <rule-config name="ObjectOverrideOnlyNonCpsMethods">
      <property name="applyToFileNames" value="*.groovy"/>
    </rule-config>

    <rule-config name="ParameterOrReturnTypeNotSerializable">
      <property name="applyToFileNames" value="*.groovy"/>
    </rule-config>

  </ruleset-ref>

</ruleset>
